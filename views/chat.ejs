<section class="mt-4 mb-4 w-full flex flex-col flex-1 px-4 sm:px-6 max-w-full lg:max-w-5xl mx-auto text-[17px] sm:text-sm">


<!-- Info + Botones en la misma fila en desktop, en columna en móvil -->
<div class="flex flex-col sm:flex-row sm:items-center sm:justify-between gap-3 w-full mb-4">

  <!-- Info -->
  <div class="bg-blue-100 border border-blue-300 text-blue-800 p-2 rounded-lg text-base sm:text-sm w-full sm:flex-1">
    🧐 Este chat guarda tus preguntas y respuestas de forma temporal en tu navegador.
  </div>

  <!-- Botón Reiniciar -->
  <button id="btn-reset" class="w-full sm:w-auto bg-white border border-gray-300 text-gray-700 px-5 py-2 rounded-md hover:bg-gray-100 shadow transition font-medium text-base sm:text-sm">
    🗑️ Reiniciar chat
  </button>

  <!-- Botón Descargar PDF -->
  <button id="btn-descargar-pdf" class="w-full sm:w-auto bg-white border border-gray-300 text-gray-700 px-5 py-2 rounded-md hover:bg-gray-100 shadow transition font-medium text-base sm:text-sm flex items-center justify-center gap-2">
    <svg xmlns="http://www.w3.org/2000/svg" class="w-5 h-5 sm:w-4 sm:h-4" fill="none" viewBox="0 0 24 24" stroke="currentColor">
      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 4v16m0 0l-6-6m6 6l6-6"/>
    </svg>
    Descargar PDF
  </button>
</div>


<!-- Chat -->
<div id="chat-box" class="flex-1 min-h-[300px] overflow-y-auto space-y-5 mb-4 border border-gray-300 rounded-xl p-5 bg-white shadow-inner scroll-smooth">
<!-- mensajes -->
</div>


<!-- Indicador escribiendo -->
<div id="asistente-escribiendo" class="hidden text-[16px] text-gray-500 animate-pulse mb-2 px-1 font-mono">
<span class="text-red-600 font-bold">Asistente:</span>
<span class="ml-2">escribiendo<span class="dots">...</span></span>
</div>


<!-- Formulario chat -->
<form id="form-chat" class="relative bg-white border border-gray-300 rounded-full shadow px-4 flex items-center w-full mt-2">
<textarea name="pregunta" id="pregunta" rows="1" required
 placeholder="Escribí tu consulta y presioná Enter..."
 class="flex-1 resize-none overflow-hidden rounded-full p-4 text-[17px] sm:text-sm placeholder:text-sm sm:placeholder:text-base focus:outline-none focus:ring-0 border-none leading-snug"
 style="min-height: 56px; max-height: 160px;"></textarea>


<button type="submit" class="ml-2 text-white bg-red-600 hover:bg-red-700 transition rounded-full w-12 h-12 flex items-center justify-center shadow-md">
<svg xmlns="http://www.w3.org/2000/svg" class="h-6 w-6" fill="none" viewBox="0 0 24 24" stroke="currentColor">
<path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 12h14M12 5l7 7-7 7" />
</svg>
</button>
</form>
</section>

<!-- Modal reinicio -->
<div id="modal-confirmar-reset" class="fixed inset-0 bg-black bg-opacity-50 flex items-center justify-center z-50 hidden">
  <div class="bg-white rounded-lg p-6 shadow-lg w-full max-w-sm space-y-4">
    <h3 class="text-lg font-semibold text-gray-800">¿Reiniciar conversación?</h3>
    <p class="text-sm text-gray-600">Se eliminará todo el historial guardado en esta sesión.</p>
    <div class="flex justify-end gap-2 pt-2">
      <button id="cancelar-reset" class="px-4 py-1 rounded border border-gray-300 text-gray-700 hover:bg-gray-50">Cancelar</button>
      <button id="confirmar-reset" class="px-4 py-1 rounded bg-red-600 text-white hover:bg-red-700">Reiniciar</button>
    </div>
  </div>
</div>

<style>
  .animate-fade-in { opacity: 0; animation: fadeIn 0.5s ease-in forwards; }
  @keyframes fadeIn { to { opacity: 1; } }

  .dots::after {
    content: '';
    display: inline-block;
    width: 1em;
    animation: dots 1s steps(3, end) infinite;
  }
  @keyframes dots {
    0% { content: ''; }
    33% { content: '.'; }
    66% { content: '..'; }
    100% { content: '...' }
  }
</style>

<script src="/js/chatFrontend.js"></script>
<script src="https://cdnjs.cloudflare.com/ajax/libs/jspdf/2.5.1/jspdf.umd.min.js"></script>

<!-- Módulo PDF -->
<script type="module">
  import { generarPDFdeChat } from '/js/chatPDF.js';
  const btn = document.getElementById("btn-descargar-pdf");
  if (btn) btn.addEventListener("click", generarPDFdeChat);
</script>